export enum Sprites {
  WIZARD = "wizard",
  EXPLORER = "explorer",
  HERO = "hero",
  RULER = "ruler",
  ARTIST = "artist",
  CAREGIVER = "caregiver",
  COMMON_MAN = "commonMan",
  INNOCENT = "innocent",
  JOKER = "joker",
  LOVER = "lover",
  OUTLAW = "outlaw",
  SAGE = "sage",
}

export enum Tilesets {
  DESERT_GATE = "desert_gate",
}

export enum Maps {
  DESERT_GATE = "desertgate",
}

export enum Layers {
  DESERT_GATE = "desert_gate",
  OBJECTS = "HEAD",
}

export enum Objects {
  SPAWN = "spawn",
  DOOR = "door",
  NPC = "npc",
  DIALOG = "dialog",
}

export enum AssetExtension {
  JSON = "json",
  WEBP = "webp",
}

export enum Audios {
  OPENING = "opening",
  DOOR = "door",
  CLICK = "click",
}
